লাইব্রেরি								কেন দরকার
numpy				ইমেজ numpy অ্যারে বানাতে ও প্রসেস করতে
os				ফাইলপাথ/ডিরেক্টরি access করার জন্য
cv2				ইমেজ লোড, রিসাইজ ও BGR → RGB কনভার্সনের জন্য
matplotlib.pyplot		ইমেজ ও ট্রেনিং গ্রাফ দেখানোর জন্য
tensorflow			পুরো deep learning ফ্রেমওয়ার্ক, including datasets, training, etc.
keras (TensorFlow এর অংশ)		Sequential model, Layers, Callbacks, Dataset loading


🔷 AUTOTUNE = tf.data.AUTOTUNE
কাজ: TensorFlow এর tf.data pipeline-এর performance optimize করার জন্য।

কেন ব্যবহৃত হয়:

যখন তুমি .map() বা .prefetch() ব্যবহার করো, তখন কতগুলো ডেটা একসাথে লোড হবে সেটার সংখ্যা যদি অটো সিলেক্ট করতে চাও — তখন AUTOTUNE ব্যবহার করা হয়।

উদাহরণ ব্যবহার:

ds.prefetch(buffer_size=AUTOTUNE)
উপকারিতা:

ডেটা লোড আর ট্রেনিং একসাথে efficiently চলে

GPU idle time কমে যায়

🔷 BATCH_SIZE = 16
কাজ: একবারে মডেলে কতগুলো ইমেজ প্রসেস হবে তা নির্ধারণ করে।

কেন ব্যবহৃত হয়:

বড় batch → faster training but বেশি RAM লাগে

ছোট batch → কম RAM ব্যবহার হয় (low-end device এ ভালো)

তোমার ক্ষেত্রে: 16 রাখা হয়েছে যাতে কম RAM খরচ হয়

🔷 IMAGE_SIZE = (128, 128)
কাজ: সব ইমেজকে একই সাইজে রিসাইজ করার জন্য।

কেন দরকার:

CNN মডেলে input shape fixed হতে হয়

তাই সব ইমেজকে (128, 128) তে রিসাইজ করা হয়

ব্যবহার হয় কোথায়:

image_dataset_from_directory(... image_size=IMAGE_SIZE)
🔷 EPOCHS = 10
কাজ: পুরো ডেটাসেট কয়বার মডেলে ট্রেনিং হবে তা নির্ধারণ করে।

উদাহরণ: যদি তোমার ডেটাসেট 1000 ইমেজ থাকে, EPOCHS = 10 মানে পুরো 1000 ইমেজ ১০ বার মডেল ট্রেনিংয়ের জন্য দেখবে।

কম হলে: মডেল underfit করতে পারে (ভালো শেখে না)

বেশি হলে: overfit করার সম্ভাবনা থাকে

✅ সংক্ষেপে টেবিলে উপস্থাপন:
কনস্ট্যান্ট	কাজ	কেন দরকার
AUTOTUNE	ডেটা লোডিং অপ্টিমাইজ করে	পারফরমেন্স বাড়ায়
BATCH_SIZE	একসাথে কত ইমেজ ট্রেনিং হবে	RAM ব্যালান্স রাখতে
IMAGE_SIZE	ইমেজ রিসাইজ (fixed shape)	মডেলের ইনপুট shape ঠিক রাখতে
EPOCHS	কয়বার পুরো ডেটা ট্রেন হবে	শেখার পরিমাণ নির্ধারণ করে


1. Training vs Validation Accuracy
যদি দুইটা accuracy একসাথে বেড়ে যায় → Model ভালো শিখছে।

যদি validation accuracy কমতে থাকে বা থেমে যায় কিন্তু training বাড়ে → Overfitting।

2. Training vs Validation Loss
যদি দুইটিই কমে → ভালো।

যদি training কমে কিন্তু validation loss বাড়ে → Overfitting।